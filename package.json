{
  "private": true,
  "workspaces": [
    "packages/ghsa-offline",
    "packages/ghsa-offline-db",
    "packages/ghsa-offline-updater"
  ],
  "version": "0.0.1",
  "scripts": {
    "build": "tsc --build --force tsconfig.json",
    "clean": "rm packages/ghsa-offline-updater/ghsa.sqlite",
    "start": "TS_NODE_PROJECT=./tsconfig.packages.json ts-node ./packages/ghsa-offline-updater/src/index.ts"
  },
  "devDependencies": {
    "@tsconfig/node14": "1.0.0"
  }
}
